// Jetzt kannst du Arrays erstellen und deren einzelne Werte ausgeben.
// Um die Anzahl der Werte in einem Array zu ermitteln, kannst du die Array-Methode length verwenden.
// Verwende die length-Methode, um zu überprüfen, wie viele Werte sich in deinen Arrays befinden.
// Lasse das Ergebnis in der Konsole ausgeben.
// Achte darauf, welches Ergebnis dir die Methode in der Konsole anzeigt.
                    
let person = ["nancy", "kopp", "leipzig"]
console.log(person)

let friends = ["mickey", "michelle", "maurice"]
console.log(friends)

let favoriteFood = ["iceCream", "chickenSoup", "uschisNoodles"]
console.log(favoriteFood)

let kopps = [person, friends, favoriteFood]
console.log(kopps)

console.log(person.length)
console.log(friends.length)
console.log(favoriteFood.length)